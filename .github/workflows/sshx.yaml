name: SSHX Runner

on:
  workflow_dispatch:

jobs:
  sshx:
    runs-on: ubuntu-latest

    steps:
      - name: Install sshx
        run: |
          curl -sSf https://sshx.io/get | sh -s run

      - name: Keep runner alive
        run: |
          tail -f /dev/null
